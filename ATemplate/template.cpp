#include <bits/stdc++.h>
using namespace std;
#define all(x) (x).begin(), (x).end();

using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    return 0;
}